pluginManagement {
  def flutterSdkPath = {
    def p = new Properties()
    file("local.properties").withInputStream { p.load(it) }
    def path = p.getProperty("flutter.sdk")
    assert path != null : "flutter.sdk not set in local.properties"
    return path
  }()

  includeBuild("$flutterSdkPath/packages/flutter_tools/gradle")

  repositories {
    google()
    mavenCentral()
    gradlePluginPortal()
  }
}

plugins {
  // Needed by modern Flutter builds
  id "dev.flutter.flutter-plugin-loader" version "1.0.0"

  // Declare versions only — DO NOT apply to settings:
  id "com.android.application" version "8.4.2" apply false
  id "com.android.library"     version "8.4.2" apply false
  id "org.jetbrains.kotlin.android" version "1.9.24" apply false
}

include(":app")